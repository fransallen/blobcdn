{
  "name": "blobcdn",
  "description": "The CDN for beautiful SVG shapes.",
  "private": true,
  "dependencies": {
    "blobs": "^2.2.1-beta.1",
    "etag": "^1.8.1",
    "express": "^4.17.1",
    "randomcolor": "^0.6.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  },
  "scripts": {
    "dev": "nodemon index.js",
    "format": "prettier --write '**/*.{html,js,json,css}'",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
